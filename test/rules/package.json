{
  "name": "afterglow-firestore-rules-test",
  "description": "firestore.rules の回帰テスト（Firestore エミュレータ上で実行）",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-afterglow --config ../../firebase.json \"node --test *.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.27.0"
  },
  "private": true
}
